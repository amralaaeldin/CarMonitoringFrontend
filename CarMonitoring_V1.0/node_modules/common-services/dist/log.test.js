'use strict';

var _assert = _interopRequireDefault(require("assert"));

var _sinon = _interopRequireDefault(require("sinon"));

var _knifecycle = _interopRequireWildcard(require("knifecycle"));

var _log = _interopRequireDefault(require("./log"));

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { default: obj }; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('initLogService', () => {
  const debug = _sinon.default.stub();

  const logger = {
    error: _sinon.default.stub(),
    info: _sinon.default.stub()
  };
  beforeEach(() => {
    debug.reset();
    logger.info.reset();
    logger.error.reset();
  });
  test('should work', done => {
    (0, _log.default)({
      debug,
      logger
    }).then(fn => {
      (0, _assert.default)('function' === typeof fn);

      _assert.default.deepEqual(debug.args, [['👣 - Logging service initialized.']]);

      _assert.default.deepEqual(logger.info.args, []);

      _assert.default.deepEqual(logger.error.args, []);
    }).then(() => done()).catch(done);
  });
  describe('log', () => {
    test('should work', done => {
      (0, _log.default)({
        debug,
        logger
      }).then(log => {
        debug.reset();
        log('debug', 'debug test');
        log('stack', 'stack test');
        log('info', 'info test');
        log('error', 'error test');

        _assert.default.deepEqual(debug.args, [['debug test']]);

        _assert.default.deepEqual(logger.info.args, [['info test']]);

        _assert.default.deepEqual(logger.error.args, [['stack test'], ['error test']]);
      }).then(() => done()).catch(done);
    });
  });
  test('should work with Knifecycle', done => {
    new _knifecycle.default().register(_log.default).register((0, _knifecycle.constant)('debug', debug)).register((0, _knifecycle.constant)('logger', logger)).run(['log']).then(({
      log
    }) => {
      debug.reset();
      log('debug', 'debug test');
      log('info', 'info test');

      _assert.default.deepEqual(debug.args, [['debug test']]);

      _assert.default.deepEqual(logger.info.args, [['info test']]);
    }).then(() => done()).catch(done);
  });
});
//# sourceMappingURL=log.test.js.map